{% extends 'base.html.twig' %}

{% block title %}Vos prochains événements
{% endblock %}

{% block body %}
<h1>
        {% if  app.user and app.user.hasRole('ROLE_USER') %}
			Vos prochains événements
				{{ app.user.prenom }} {{ app.user.nom }} !
		{% endif %}
</h1>

<div class="row row-cols-1 row-cols-md-3">
            {% for event in events %}
            <div class="col mb-4" >
                <div class="card h-100" >
                    <img src="https://www.lamaisonbleue-stbg.com/fichiers/2020/01/jamesleg-500x500.jpg" 
                    class="card-img-top"alt="">
                    <div class="card-body">
                        <h5 class="card-title">{{ event.artiste }}</h5>
                        <p class="card-text"> Le {{ event.date| date('d/m/Y')}} à {{ event.date| date('H:i:s')}}
						<br> {{ event.presentation }} <br> {{ event.tarif }}
                            <br> {{ event.type }} <br> {{ event.adresse }}</p>	
							<div class="mt-5 text-center"><a href="#" class="btn btn-primary">📖 Lire l'event</a>
					</div>
						
                            
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}